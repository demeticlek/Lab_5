<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- First icon - always visible in toolbar -->
    <item
        android:id="@+id/item_one"
        android:title="Choice 1"
        android:icon="@drawable/ic_icon_one"
        android:orderInCategory="1"
        app:showAsAction="always" />

    <!-- Second icon - always visible in toolbar -->
    <item
        android:id="@+id/item_two"
        android:title="Choice 2"
        android:icon="@drawable/ic_favorite"
        android:orderInCategory="2"
        app:showAsAction="always" />

    <!-- Overflow item - hidden in 3-dot menu -->
    <item
        android:id="@+id/item_overflow"
        android:title="About"
        android:orderInCategory="3"
        app:showAsAction="never" />

</menu>